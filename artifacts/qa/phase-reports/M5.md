# M5 - 콘텐츠 블록 렌더러·Markdown 안전화·이미지 자산 완료 보고서

- 날짜: 2026-09-01
- 커밋 SHA: (미커밋 - 작업 트리 상태)
- 환경: Windows 11 Pro 10.0.26200 / Node 24.20.0 LTS / pnpm 11.5.3
- 새 의존성: unified 11.0.5, remark-parse 11.0.0, remark-gfm 4.0.1, remark-rehype 11.1.2, rehype-stringify 10.0.1, dompurify 3.4.14

## 구현 범위

| 경로                                     | 줄 수 | 역할                                                    |
| ---------------------------------------- | ----: | ------------------------------------------------------- |
| `src/features/sanitize/`                 |   346 | Markdown→HTML 변환(raw HTML 차단)과 DOMPurify 2차 살균  |
| `src/features/assets/`                   |   438 | 이미지 검증·최적화, SHA-256 체크섬, Blob URL 수명 훅    |
| `src/components/content/`                |   925 | BlockRenderer 외 7종. 작성기 미리보기와 리더가 공유     |
| `src/components/editor/BlockEditor/`     |   421 | 블록 타입별 폼                                          |
| `src/components/editor/BlockTypePicker/` |    88 | 고정 순서 블록 추가 목록                                |
| `src/utils/clipboard.ts`                 |    73 | Clipboard API와 선택 폴백                               |
| `scripts/verify-security.mjs`            |   145 | XSS 픽스처 전수 검증 (`pnpm test:security`)             |
| `scripts/verify-dependencies.mjs`        |    77 | 의존성 정확 버전 고정 검증 (`pnpm verify:dependencies`) |
| `scripts/generate-fixture-assets.mjs`    |   170 | 결정론적 자산 픽스처 생성                               |
| `tests/unit/{security,content,assets}/`  |   900 | 108 케이스                                              |
| `tests/e2e/content-blocks.spec.ts`       |   205 | 9 케이스 × 브라우저 3종                                 |

## DoD 결과

| #   | 항목                                                       | 결과 | 증거                                                                                                        |
| --- | ---------------------------------------------------------- | ---- | ----------------------------------------------------------------------------------------------------------- |
| 1   | 판별 유니온 exhaustive, 미지원 타입이 조용히 무시되지 않음 | 통과 | `never` 소진 검사(컴파일) + `role="alert"` 안내(런타임). 블록 7종 각각이 무언가를 그리는지 표 기반으로 확인 |
| 2   | XSS 페이로드 실행 0건                                      | 통과 | 단위 24종 + 픽스처 전체 44개 문자열 + 실제 브라우저에서 `dialog` 0건. 판정은 문자열이 아니라 DOM 검사       |
| 3   | 코드 블록이 HTML로 해석되지 않고 텍스트로 보존             | 통과 | React 텍스트 노드로 렌더링. `code.children` 0개, 코드 펜스 안 `<script>`는 `&lt;script&gt;`로 남음          |
| 4   | http·https만 허용, 외부 링크에 `noopener noreferrer`       | 통과 | 살균기 훅과 `LinkCard` 모두 `domain`의 `isAllowedUrl` 하나만 사용. 상대·프로토콜 상대 URL도 차단            |
| 5   | SVG 차단, PNG·JPEG·WebP·GIF만 처리                         | 통과 | MIME 기준 판정이라 확장자를 바꿔도 걸림. E2E에서 실제 `blocked.svg` 업로드가 막히는 것 확인                 |
| 6   | 5MB 초과 원본 차단, 빈 alt는 내보내기 오류                 | 부분 | 5MB 차단은 저장 전 판정으로 확인. **빈 alt의 내보내기 차단은 M9 범위** - 아래 "남은 경고" 참조              |
| 7   | 1920px 초과가 최적화 후 상한 이하, 더 크면 원본 유지       | 통과 | 4000×3000 → 1920×1440. 축소 불필요 + 재인코딩이 손해면 원본 유지                                            |
| 8   | 같은 checksum 자산은 Blob이 중복되지 않음                  | 통과 | SHA-256 + 저장소 트랜잭션 dedup. E2E에서 두 블록의 `data-asset-id`가 같은 것 확인                           |
| 9   | 교체·삭제·unmount 후 Blob URL 해제                         | 통과 | 세 경우를 각각 단언. `createObjectURL`/`revokeObjectURL`을 가로채 만든 주소와 해제한 주소가 일치하는지 비교 |
| 10  | Clipboard 실패 시 선택·복사 대체 동작                      | 통과 | 실패·API 부재 두 경우 모두 코드 전체를 선택하고 `role="status"`로 안내                                      |

## 실행 명령

```text
pnpm exec vitest run tests/unit/content tests/unit/security tests/unit/assets → 0  108 passed
pnpm exec playwright test tests/e2e/content-blocks.spec.ts --project=chromium → 0    9 passed
pnpm test:security                                                            → 0  문자열 44개, 잔재 0건
pnpm typecheck                                                                → 0
pnpm lint                                                                     → 0

전체 회귀
pnpm format:check          → 0
pnpm test:unit             → 0  471 passed
pnpm test:integration      → 0   68 passed
pnpm verify:architecture   → 0  소스 90개(src 61개), 규칙 6종
pnpm verify:dependencies   → 0  의존성 35개 모두 정확한 버전
pnpm verify:fixtures       → 0  가이드 10개
pnpm build                 → 0  JS 623.3 kB (gzip 199.7 kB), CSS 33.8 kB (gzip 6.5 kB)
pnpm exec playwright test  → 0   54 passed (chromium/firefox/webkit)
```

### 음성 검증

게이트가 실제로 무는지 확인했다.

```text
2단계 살균을 통과로 바꿈 (sanitizeHtml이 입력을 그대로 반환)
  → 단위 11건 실패, test:security가 javascript: href를 잡아 종료 코드 1

살균 경계 밖에 dangerouslySetInnerHTML 주입
  → verify:architecture SANITIZE_BOUNDARY, 종료 코드 1

xss 픽스처에서 srcdoc 페이로드 제거
  → test:security "픽스처에 페이로드가 없습니다", 종료 코드 1

package.json 한 항목을 캐럿 범위로 되돌림
  → verify:dependencies, 종료 코드 1
```

## 설계 판단

**살균은 두 겹이다.** 1단계는 `remark-rehype`에 `allowDangerousHtml`을 주지 않는 것 - 원문의 raw HTML 노드가 AST에서 통째로 사라진다. 2단계는 DOMPurify다. 1단계만으로 충분해 보여도 뺄 수 없다. remark 플러그인이 바뀌거나 새 노드 타입이 생기면 1단계의 가정이 조용히 무너진다. 실제로 2단계를 통과로 바꾸자 11건이 실패했다.

**정책은 허용 목록이다.** `ALLOWED_TAGS`/`ALLOWED_ATTR`가 기준이고 `FORBID_*`는 그 위의 이중 확인이다. 금지 목록만 두면 새 태그가 나올 때마다 구멍이 생긴다.

**원격 이미지를 렌더링하지 않는다.** Markdown의 `![](https://…)`는 `src`를 지우고 `alt`만 남긴다. 그리면 독자가 문서를 여는 순간 제3자에게 접속 사실이 새어 나간다(INV-15). 차단 건수를 호출자에게 돌려줘 편집기가 안내할 수 있게 했다.

**URL 프로토콜 판정은 `domain`이 단독 소유한다.** 이번에 `isAllowedUrl`을 `guide.schema.ts`에서 `guide.types.ts`로 옮겼다. 스키마 파일에 두면 살균기와 링크 렌더러가 그 함수를 쓰려고 zod를 함께 끌어오고, 그 의존이 나중에 리더 번들까지 따라간다. (D-11)

## 이번 phase에서 드러난 것

- **`ALLOWED_URI_REGEXP`를 좁히면 URI가 아닌 속성도 사라진다.** DOMPurify는 자기 `URI_SAFE_ATTRIBUTES` 목록에 없는 속성 값을 전부 이 정규식에 통과시킨다. http/blob/data:image만 허용하도록 좁혔더니 `type="checkbox"`·`colspan="2"` 같은 평범한 값이 조용히 사라졌다. GFM 작업 목록 체크박스가 통째로 없어지는 것을 보고 알았고, `ADD_URI_SAFE_ATTR`로 비-URI 속성을 명시했다.
- **Safari에서 이미지 첨부가 실패했다.** WebKit E2E 2건이 미리보기가 뜨지 않아 실패했다. `OffscreenCanvas.convertToBlob`이 없는 빌드가 있는데 인코더가 그것 하나만 썼다. 일반 `<canvas>` + `toBlob()` 폴백과 EXIF 옵션 없는 `createImageBitmap` 폴백을 넣었다. 코덱 실패는 예외로 새어 나가지 않고 사용자가 볼 수 있는 이슈(`IMAGE_PROCESSING_FAILED`)가 된다. **브라우저 3종 E2E가 없었으면 Safari 사용자에게만 조용히 깨진 채로 나갔다.**
- **의존성 버전 고정이 M4부터 새고 있었다.** `.npmrc`에 `save-exact=true`가 있는데도 M4의 zustand·react-hook-form과 M5의 6종이 전부 캐럿 범위로 들어왔다. 어떤 게이트도 보지 않아 두 phase가 그대로 통과했다. 전부 정확한 버전으로 고정하고 `verify:dependencies`를 만들었다. 설정은 의도이고 검사는 사실이다.

## 성능·접근성·보안 수치

- **편집기 초기 JS**: gzip **199.7 kB**. 기술 §2.4.2 목표 300 kB 이하, 최대 500 kB - 목표 안. M4의 134.2 kB에서 65.5 kB 늘었고 전부 unified/remark/rehype/DOMPurify다.
- **CSS**: gzip 6.53 kB (M4 5.06 kB).
- **접근성**: 코드 블록은 가로 스크롤 영역이라 `tabindex=0` + `role="group"`으로 키보드 스크롤이 가능하다(WCAG 2.1.1). 체크리스트·선택지의 라벨 행은 최소 44px. 복사 결과는 `role="status"`로 전달한다. 빈 `alt`는 `role="presentation"`으로 보조 기술에서 숨긴다.
- **보안**: `dangerouslySetInnerHTML`은 `components/content/MarkdownText/` 한 곳뿐이며 `verify:architecture`가 강제한다. 그 컴포넌트는 살균된 HTML이 아니라 **원문 Markdown**을 받아 스스로 살균한다. "이미 안전한 문자열"을 넘기는 경로 자체를 두지 않았다.

## 남은 경고

- **DoD 6의 빈 alt 차단은 M9 범위다.** 편집기는 "비워 두면 장식용으로 봅니다"라고 안내하고 `role="presentation"`으로 렌더링하지만, 장식용이 아닌데 비어 있는 경우를 **오류로 만드는 것은 내보내기 검증**의 일이다(기술 §2.2.4 내보내기 검증). 이슈 코드 `IMAGE_ALT_REQUIRED`는 M2에 이미 있고, `features/export-html/export-validator.ts`가 M9에서 판정한다. 지금은 `ImageBlock`에 장식용 플래그가 없어 빈 문자열을 선언으로 해석한다(PROGRESS 미결 항목).
- **리더 런타임과 살균기의 의존 경계가 아직 결정되지 않았다.** `File_Structure.md` §3.3은 "내보낸 HTML은 `reader-runtime/reader-renderer.ts`가 직접 이 순수 함수를 호출한다"고 적었지만, `features/sanitize`는 지금 unified·remark·DOMPurify에 의존한다. `READER_RUNTIME_ALLOWED_PACKAGES`는 기본 거부라 M7·M9에서 리더가 이것을 import하는 순간 D-11 전이 검사가 막는다. 아직 `reader-runtime/`이 비어 있어 게이트가 울리지 않았을 뿐이다. **M7 전에 결정해야 한다.** PROGRESS 미결 항목에 선택지를 적어 두었다.
- **`photo-large.jpg`가 `photo-large.png`다.** 하네스 §0.10의 파일명 대신 PNG를 만든다. JPEG 인코더를 직접 쓰는 것은 "의존성 없이 결정론적"이라는 생성 스크립트의 목적과 맞지 않는다. File_Structure.md §5가 "이름은 재량이고 역할은 계약"이라고 명시하며, 역할(긴 변 상한 검증)은 그대로다. 1.2MB라 커밋하지 않고 `.gitignore`에 두었다.
- **체크리스트·선택지의 항목 추가·삭제 UI가 없다.** 기존 항목 편집만 가능하다. 분기 편집과 함께 M6에서 붙인다.
- **Markdown 렌더링에 문법 강조가 없다.** 하이라이터는 코드에서 HTML을 만들어 INV-07의 표면을 넓힌다. MVP는 하지 않는다.

## 다음 phase 진입조건 확인

- [x] 선행 게이트 전부 PASS (M1~M5 검증 블록과 전체 회귀)
- [x] 미해결 불변식 위반 없음 (INV-07·INV-15는 세 겹의 게이트로 고정)
- [x] PROGRESS.md 갱신
