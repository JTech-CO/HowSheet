/**
 * 생성 프롬프트가 언제나 요구하는 것과 언제나 금지하는 것.
 *
 * 기준: v2 제품정의 §5.1(금지 목록), §8 INV-06·INV-08. 하네스 P3 할 일 2·3.
 *
 * ## 왜 문자열 하나가 아니라 배열인가
 *
 * 금지 목록을 문단 하나로 뭉쳐 두면 "그 문단이 들어갔는가"만 검사하게 되고,
 * 그러면 **목록이 비어도 통과한다.** 항목 단위로 세고 항목 단위로 검사하려면
 * 항목이 각각 값이어야 한다. (하네스 P3 주의)
 *
 * 이 목록은 P3의 템플릿 조립기와 P5의 AI 합성이 함께 쓴다. 두 곳에 각자 적으면
 * 한쪽만 고쳐지는 날이 오고, 그날 INV-06이 조용히 깨진다.
 */

/**
 * 디자인 축과 **무관하게** 모든 프롬프트에 들어가는 금지 항목.
 *
 * 이것이 이 제품의 값어치다. 그냥 "예쁘게 만들어 줘"라고 하면 어느 AI든 같은
 * 결과를 낸다. 제품정의 §5.1의 일곱 줄을 그대로 옮긴다.
 *
 * **줄이지 않는다.** 항목을 빼는 변경은 INV-06을 약화시키는 변경이다.
 */
export const PROHIBITIONS: readonly string[] = [
  '네온 글로우, 여러 색을 섞은 그라데이션, 그라데이션으로 칠한 글자를 쓰지 않는다.',
  '글래스모피즘(`backdrop-blur`)을 남용하지 않는다. 겹친 층이 본문을 읽기 어렵게 만든다.',
  '제목에 장식용 이모지를 붙이지 않는다. (`✨ 기능`, `🚀 시작하기` 같은 것)',
  '떠다니는 파티클이나 blob 배경, 마우스를 따라다니는 그라데이션을 넣지 않는다.',
  'shimmer·marquee·glitch 효과를 쓰지 않고, 호버에서 요소를 크게 확대하거나 회전시키지 않는다.',
  // 코드에 쓰는 고정폭은 세지 않는다. 세면 '혼합' 타이포(제목 세리프·본문
  // 산세리프)와 기술 톤(코드 고정폭)이 같은 프롬프트에서 이 줄과 모순된다.
  // (2026-09-15 사용자 확정, P8에서 발견)
  '제목과 본문의 폰트 계열을 셋 이상 섞지 않는다. 코드에 쓰는 고정폭 글꼴은 이 수에 넣지 않는다.',
  '외부 CDN·웹폰트·분석 스크립트를 부르지 않는다. 결과물이 오프라인에서 열려야 한다.',
];

/**
 * 결과물이 갖춰야 하는 것.
 *
 * 하네스 P3 할 일 3이 네 가지를 이름으로 지정한다 - 단일 HTML, 외부 요청 0건,
 * 인쇄, 320px. 그 넷을 그대로 둔다. (INV-08, INV-10)
 */
export const OUTPUT_REQUIREMENTS: readonly string[] = [
  '전체를 **HTML 파일 하나**로 낸다. CSS와 스크립트를 그 파일 안에 인라인으로 넣고 별도 파일을 만들지 않는다.',
  '외부 요청이 **0건**이어야 한다. 파일을 오프라인에서 열어도 똑같이 보여야 한다.',
  '인쇄할 수 있어야 한다. `@media print`에서 배경을 걷어내고, 표와 코드 블럭이 페이지 경계에서 잘리지 않게 한다.',
  '가로 폭 320px에서 가로 스크롤이 생기지 않아야 한다. 표·코드·그림처럼 넓은 것은 그 요소 자신이 스크롤한다.',
];
